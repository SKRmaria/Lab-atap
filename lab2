#include <stdio.h>
#include <math.h>

int main() {
    double r1, r2, a, p;
    scanf("%lf", &r1);
    scanf("%lf", &r2);
    p = 3.14;
    
   if(r2 > r1){
    a = (p*pow(r2, 2))-(p*pow(r1, 2));
    printf(" %lg\n", a);
} else {
    printf("r2 повинне бути більше r1");
}
    
    return 0;
}

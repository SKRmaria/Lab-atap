#include <stdio.h>
#include <math.h>

int main() {
    double x, y, z, a;
    scanf("%lf", &x);
    scanf("%lf", &y);
    scanf("%lf", &z);
    a = sqrt((x*y)+(x*z)+(y*z));
    printf("%lg\n", a);
    return 0;
}

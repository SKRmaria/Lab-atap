#include <stdio.h>
#include <math.h>

int main() {
    double z1, z2, a, b;
    scanf("%lf", &a);
    scanf("%lf", &b);
    z1 = pow(cos(a)-cos(b), 2)-pow(sin(a)-sin(b), 2);
    printf("%lg\n", z1);
    z2 = -4*pow(sin((a-b)/2), 2)*cos(a+b);
    printf("%lg\n", z2);

    return 0;
}
